# AI agent guidance

For a full or partial migration of an existing mini-program or application into
the React Native `htyf` target, use the `$htyf-migration` skill. It governs
feature parity, native dependencies, in-tree overlays, capsule and safe-area
layout, template sourcing, tests, and migration acceptance.
